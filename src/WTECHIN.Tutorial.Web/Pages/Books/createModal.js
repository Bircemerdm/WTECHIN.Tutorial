var abp = abp || {};
abp.modals.bookCreate = function () { //abp.modals ABP Framework'te kullanılan modal (açılır pencere) işlemlerini yöneten bir nesne.
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("Tutorial");


    };

    return {
        initModal: initModal
    };
};

